#!/usr/bin/env node
// Moved to global: C:/Users/LMOperations/.claude/scripts/textme.mjs
// This project-level copy is no longer used.
// The global Stop hook in ~/.claude/settings.local.json handles all projects.
console.error('[textme] This script moved to ~/.claude/scripts/textme.mjs');
process.exit(0);
